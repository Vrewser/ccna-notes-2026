# Modify Single-Area OSPFv2

---

## Cisco OSPF Cost Metric
* A metric gives indication of the overhead that is required to send packets across a certain interface.
* *OSPF uses cost as a metric.*

*note: A lower cost indicates a better path than a higher cost.*

**Cost**
* Reference Bandwidth / Interface Bandwidth
* The default reference bandwidth is 10^8 (100,000,000).
* The formula being:
    * Cost = 100,000,000 bps / *interface bandwidth in bps*

![Cost Calculation Breakdown](<../../Images/Screenshot 2026-09-03 090652.png>)

* Since the OSPF cost value must be an integer, FastEthernet, Gigabit Ethernet, and 10 Gigabit Ether net (10 GigE) interfaces share the same cost.
* To fix this:
    * Adjust the reference bandwidth with `auto-cost reference-bandwidth` on each OSPF routers.
    * Manually set the OSPF cost value with the `ip ospf cost` command.

--- 

## Adjust the Reference Bandwidth
* The cost value must be an integer, if something less than an integer is calculated the OSPF round up to the nearest integer.
    * Cost = 100,000,000 bps / 1,000,000,000 = 1

**To assist OSPF in making the correct path determination:**
* `auto-cost reference-bandwidth MBPS`

![Cost Table](<../../Images/Screenshot 2026-09-03 091731.png>)

<br>

---

## Hello Packet Intervals
* The default timer value on multiaccess and point-to-point networks is: *every 10 seconds*

---

## Modify OSPFv2 Intervals
* Changing the OSPF timers may be desirable, so that routers detect network failures in less time.
* Doing this may increase traffic but a quick convergence is more important than the extra traffic.

**The commmand used to modify the interval is:**
* Hello Intervals - `ip ospf hello-interval seconds`
* Dead Intervals - `ip ospf dead-interval seconds`
* just add `no` at the start of each command to reset the intervals to their defualt timers.

[Packet Tracer Sample](../../PKAs/2.4.11-packet-tracer---modify-single-area-ospfv2.pka_en-US_67395d55-b4cf-48d7-99ff-5092dc7765ed.pka)

---

